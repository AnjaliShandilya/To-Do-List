# To-Do-List
This is a simple, interactive To-Do List application built using HTML, CSS, Bootstrap, and jQuery. The application allows users to add, complete, and delete tasks dynamically. It also persists tasks between sessions using localStorage, ensuring that your tasks are saved even when you refresh the page or close the browser.
